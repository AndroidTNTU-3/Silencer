<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.tntu.silenceapp"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="edu.tntu.silenceapp.MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.tntu.silenceapp.TaskPane"
            android:label="@string/title_activity_task_pane"></activity>
        <activity
            android:name="edu.tntu.silenceapp.TaskPaneEdit"
            android:label="@string/title_activity_task_pane_edit"></activity>


        <receiver android:name="edu.tntu.silenceapp.OnSoundReceiver"></receiver>
        <receiver android:name="edu.tntu.silenceapp.OffSoundReceiver"></receiver>
        <receiver android:name="edu.tntu.silenceapp.StateSoundReceiver">
            <intent-filter>
                <action android:name="android.media.RINGER_MODE_CHANGED" />
            </intent-filter>
        </receiver>

        <service android:name="edu.tntu.silenceapp.MyService"></service>

    </application>

</manifest>
